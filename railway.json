{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && rm -rf backend/public && mkdir -p backend/public && cp -R dist/. backend/public/ && npm --prefix backend ci && npm --prefix backend run build"
  },
  "deploy": {
    "startCommand": "npm --prefix backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
